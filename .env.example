# 本 App 在设备上直连模型 API（默认 DeepSeek），无需服务端环境变量。
# 打开 App → 设置页，填入 API Key 即可开始使用。

# 设为 1 时使用内置假数据调试 UI（不联网）
EXPO_PUBLIC_MOCK_AI=0

# 以下为可选项：只有当你想把 src/app/api/chat+api.ts 部署成服务端模式（Web）时才需要
ANTHROPIC_API_KEY=sk-xxx
EXPO_UNSTABLE_DEPLOY_SERVER=1
